# No environment variables required for local development
# API keys are entered by users in the app's Settings page
#
# For Vercel deployment, no server-side env vars are needed either
# The app uses client-side API key storage
